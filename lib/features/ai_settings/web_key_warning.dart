const webApiKeyWarning =
    "On web browsers, we can't fully protect your API key the way we can on "
    "the app — anyone with access to this device/browser session could "
    "potentially view it. Consider a key with provider-side spending limits.";
