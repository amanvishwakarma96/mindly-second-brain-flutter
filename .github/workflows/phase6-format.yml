name: Phase 6 Format

on:
  push:
    branches:
      - agent/phase-6-proactive-insights

permissions:
  contents: write

env:
  FLUTTER_VERSION: '3.44.8'

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Format deterministic insight test
        run: dart format test/features/insights/proactive_insight_service_test.dart
      - name: Commit formatter output
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- test/features/insights/proactive_insight_service_test.dart; then
            echo "No formatter changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add test/features/insights/proactive_insight_service_test.dart
          git commit -m "Format Phase 6 ordering test"
          git push
